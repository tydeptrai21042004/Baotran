# Remove instead of run

To avoid spending GPU time on low-value or redundant gaps, the current cleanup plan is:

- Remove the C11 100-epoch Oxford-IIIT Pet / EfficientNet-B0 tables instead of completing all missing C11 methods.
- Remove the fully blank N01–N04 authoring templates.
- Remove Prompt/VPT blank rows from CNN-only C10/C11 tables.
- Keep the current F04 figure without forcing the two missing methods.
- Do not add more dense-prediction runs; D03–D07 are already sufficiently populated.
